type Query {
    authors: [Author]
    authorById(id: ID!): Author
    save: Author
}

type Author {
    id: ID!
    name: String!
}

type Book {
    id: ID!
    title: String!
    publisher: String
}